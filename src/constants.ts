/**
 * Filename used by XUnit's TRX logger to which test results should be output.
 */
export const XUNIT_RESULTS_FILENAME = 'xunit-results.trx';

/**
 * Directory name to which XUnit outputs test result files.
 */
export const XUNIT_RESULTS_OUTPUT_DIR = 'TestResults';